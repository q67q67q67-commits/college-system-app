# Контекст проекта Narxoz College (NC)

> **Назначение файла:** сохранять актуальный контекст проекта после каждого создания, переписывания или редактирования. При новом чате — читать этот файл первым для быстрого ознакомления с проектом, условиями и текущим состоянием. Обновлять `context.md` после значимых изменений.

---

## 1. О проекте

**Название:** Narxoz College (NC) — мобильное и веб-приложение для студентов и сотрудников колледжа.

**Целевая организация:** Колледж НАРХОЗ (Narxoz College), Казахстан.  
**Официальный сайт:** https://collegenarxoz.kz/

**Суть:** Единая платформа с журналом оценок, расписанием, форумом, чатами, библиотекой, событиями, файловым хранилищем (2 ГБ на пользователя) и профилями. Роли: Студент, Преподаватель, Директор, Админ. Адаптивный UI (Web + Mobile).

**Стек:**
- **Frontend:** Flutter (мобильная + веб, адаптация под ПК и смартфоны). Разрабатывается **отдельным участником (Ф)**.
- **Backend:** Go. Разрабатывается **текущим участником (Б)**.
- **БД:** PostgreSQL (любой SQL по ТЗ).

**Важно:** Официальной базы от колледжа пока нет — используется псевдо-база с тестовыми/реалистичными данными. Данные для сидинга можно брать с сайта колледжа (специальности, имена руководителей, факты).

**Репозиторий (GitHub):**  
https://github.com/q67q67q67-commits/college-system-app  
Клонирование: `git clone https://github.com/q67q67q67-commits/college-system-app.git`  
Рабочая ветка бэкенда: **Backend**. Код бэкенда пушится в эту ветку; слияние в main — через Pull Request.

---

## 2. Разделение ответственности: только Участник Б (Backend)

- **В этом репозитории выполняются только шаги Участника Б (Backend — Go + SQL).**
- **Участник Ф (Frontend — Flutter)** работает отдельно; к фронту не приступать в рамках бэкенд-задач.
- Чтобы избежать конфликтов и дублирования:
  - Б предоставляет стабильные API-эндпоинты, контракты (JSON), документацию (Swagger/список эндпоинтов).
  - Ф только потребляет API; не менять схему БД, не писать бэкенд-логику.
  - Все изменения контрактов API согласовывать и отражать в `context.md` и в `api/` (документация).

---

## 3. Роли и права (кратко)

| Роль в коде | Описание | Особенности |
|-------------|----------|-------------|
| `student` | Студент | Журнал, расписание, GPA, транскрипт, кредиты, форум, чаты, библиотека, файлы 2 ГБ, профиль. |
| `teacher` | Преподаватель | Учебная нагрузка: журнал, ДЗ, расписание, группы, библиотека; форум, чаты, события; профиль, файлы. |
| `director` | Директор | Всё как у препод + CRUD новостей/событий. Профиль отображается всем как «Twitter-аккаунт» (шапка, фото, имя, описание, посты, закреплённые). |
| `admin` | Админ | Как директор, но **без** страницы профиля директора; админ-панель, CRUD новостей/событий. |

**Дополнительно:**
- Доступ к аккаунту сохраняется до **1 года** после ухода из заведения (поле `users.left_at`).
- Музыка в приложении не предусмотрена (авторские права).

---

## 4. Фаза 1 — Участник Б (Backend). Чек-лист

Выполняются **только** эти шаги в рамках бэкенда.

1. **[Б] Инициализация окружения**
   - Корневая структура: `/cmd`, `/internal`, `/api`.
   - `docker-compose.yml` для PostgreSQL.
   - **Статус:** выполнено.

2. **[Б] Архитектура БД (SQL)**
   - Схема: `users` (с ролями), `groups`, `schedules`, `grades`, `forum_posts`, `library_books`, `events` (+ вспомогательные таблицы).
   - Поля для анонимности в форуме и лимитов хранилища (2 ГБ).
   - **Статус:** выполнено (`migrations/001_schema.sql`).

3. **[Б] Псевдо-база (Seeding)**
   - Скрипт заполнения: группы, студенты, преподаватели, директор.
   - **Статус:** выполнено (`migrations/002_seed.sql`). Пароль тестовых аккаунтов: `password123`.

4. **[Б] Система авторизации**
   - `POST /auth/login`.
   - Middleware проверки JWT по ролям (student, teacher, director, admin).
   - **Статус:** выполнено (`internal/auth`, middleware RequireAuth/RequireRole).

5. **[Б] Основной API (CRUD)**
   - Для студента: расписание, GPA, транскрипт, статус кредитов.
   - Для преподавателя: выставление оценок, управление ДЗ, список групп.
   - Библиотека: поиск книг, бронирование, указание владельца экземпляра.
   - **Статус:** выполнено (см. `api/ENDPOINTS.md`).

6. **[Б] Социум и файлы**
   - API форума (в т.ч. анонимные посты).
   - Чаты (WebSocket).
   - Сервис загрузки файлов с проверкой квоты 2 ГБ.
   - **Статус:** выполнено (форум, WebSocket `/api/chat/ws`, загрузка файлов, таблица `user_files`, миграция `003_user_files.sql`).

7. **[Б] Административные функции**
   - API добавления/редактирования новостей и событий (права: Админ, Директор).
   - **Статус:** выполнено (POST/PUT/DELETE /api/events, middleware RequireRole admin/director).

8. **[Б] Документация**
   - Swagger или список эндпоинтов с примерами JSON для фронтенда.
   - **Статус:** выполнено (`api/ENDPOINTS.md` — список эндпоинтов с примерами JSON).

---

## 5. Фаза 2 — Участник Ф (Frontend). Только справка

Эти шаги выполняет **другой участник (Ф)**. В рамках Б не реализовывать; учитывать при проектировании API.

- **[Ф] Стартовый UI и навигация:** Flutter, адаптив (Sidebar/BottomNav). Подробно: **`docs/FRONTEND_TUTORIAL.md`**, задача 9.
- **[Ф] Модуль Auth:** экран входа, сохранение токена, редирект по роли. Задача 10.
- **[Ф] Главный экран:** лента новостей, поиск по дате/словам, CRUD для Директор/Админ. Задача 11.
- **[Ф] Учебный процесс:** студент (Журнал, Расписание, GPA, Регламент, Библиотека); препод (оценки, группы, нагрузка). Задача 12.
- **[Ф] Социум:** форум (переключатель «Анонимно»), чаты, карта здания, лента событий с комментариями. Задача 13.
- **[Ф] Профили и файлы:** профиль, смена пароля, язык; «Мои файлы» (папки, портфолио); профиль директора — «Twitter-аккаунт». Задача 14.
- **[Ф] Финализация:** тестирование ролей, проверка адаптации ПК/смартфон. Задача 15.

---

## 6. Текущая структура репозитория

```
NC/
├── cmd/api/main.go          # Точка входа API (Go 1.22+, роуты, JWT, WebSocket)
├── internal/
│   ├── auth/                # JWT (CreateToken, ValidateToken), middleware RequireAuth/RequireRole
│   ├── chat/                # WebSocket hub для чата
│   ├── config/              # Конфиг из env (NC_ADDR, NC_DB_URL, NC_JWT_SECRET, NC_UPLOAD_PATH)
│   ├── db/                  # Подключение к PostgreSQL
│   ├── handler/             # HTTP-хендлеры (auth, schedule, grades, groups, library, forum, events, files, homework)
│   └── repository/          # Запросы к БД (user, schedule, grades, groups, library, forum, events, user_storage)
├── api/
│   └── ENDPOINTS.md         # Список эндпоинтов с примерами JSON для фронтенда
├── design-prototypes/       # Прототипы из Figma (импорт макетов)
├── docs/
│   ├── GITHUB_TUTORIAL.md    # Туториал: GitHub с нуля, ветки, работа вдвоём
│   └── FRONTEND_TUTORIAL.md  # Туториал для фронтенда: Фаза 2, задачи 9–15, API
├── migrations/
│   ├── 001_schema.sql       # Базовая схема БД + триггеры updated_at
│   ├── 002_seed.sql         # Псевдо-база: группы, пользователи, расписание, оценки, форум, библиотека, события
│   └── 003_user_files.sql   # Таблица user_files и триггер обновления storage_used_bytes
├── docker-compose.yml       # PostgreSQL 16
├── .gitignore
├── go.mod                   # Go 1.22
├── ARCHITECTURE.md
├── context.md
└── Заметки.md
```

---

## 7. База данных (краткая выжимка)

- **PostgreSQL 16.** Подключение: пользователь `nc`, БД `nc_db`, пароль `nc_dev`, порт `5432`. Миграции из `migrations/` выполняются при первом запуске контейнера (`docker-entrypoint-initdb.d`).

**Основные таблицы:**

| Таблица | Назначение |
|--------|------------|
| `users` | Роль (`user_role`: student, teacher, director, admin), `storage_limit_bytes` = 2 ГБ, `storage_used_bytes`, `left_at`, язык. |
| `groups` | Учебные группы. |
| `user_groups` | Связь пользователь–группа (в т.ч. куратор). |
| `schedules` | Расписание (группа, преподаватель, предмет, день недели, время, аудитория). |
| `schedule_attachments` | ДЗ, уведомления, материалы к парам. |
| `grades` | Журнал оценок (студент, занятие, оценка, дата). |
| `forum_posts` | Посты форума: `is_anonymous`, `author_id` (для модерации при анонимности). |
| `library_books` | Книги (в т.ч. наличие физического экземпляра). |
| `library_copies` | Экземпляры, владелец, срок возврата. |
| `library_reservations` | Бронирование книг. |
| `events` | События/мероприятия (главная, лента). |
| `user_files` | Файлы пользователей (Мои файлы); триггер обновляет `users.storage_used_bytes`. |

**Важные правила:**
- Лимит хранилища: 2 ГБ = `2147483648` байт в `users.storage_limit_bytes`.
- Анонимность форума: `forum_posts.is_anonymous`; при `true` автор не отображается, `author_id` хранится для модерации.

Полная схема — в `migrations/001_schema.sql`.

---

## 8. Данные с сайта колледжа (для сидинга и контекста)

Источник: https://collegenarxoz.kz/

**Колледж в цифрах (ориентир для псевдо-базы):**
- Студенты: 1053
- Выпускники: 11154
- Аудитории: 56
- Специальности: 7

**Специальности (коды и названия):**
- 04120100 — Банковское и страховое дело
- 04140100 — Учёт и аудит
- 04130100 — Менеджмент
- 04140100 — Маркетинг
- 04210100 — Правоведение
- 06130100 — Программное обеспечение
- 06120100 — Вычислительная техника и информационные сети (по видам)

**Текущий директор (для роли director и профиля «Twitter»):**
- Абайдуллаев Мақсат Серікболұлы (Abaydullaev Maksat Serikboluly)  
- Контакт: WhatsApp +7 706 808 00 02

**История руководителей (для справки/истории):**
- Сальменбаев Николай Ахметжанович (2002–2005)
- Маянлаева Гульмира Исмаиловна (2005–2006)
- Есмуканов Карим Есмуканович (2006–2010)
- Абиров Жанибек Асылбекович (2011–2016)
- Сатаев Санат Акылжанулы (2017–2024)

**Примеры новостей (тематика для событий/ленты):**
- Навигатор в мире финансов — путь к успешной карьере
- Popeyes & Narxoz College — партнёрство
- Мероприятия по повышению качества образования
- Встречи с представителями компаний, семинары (MyBuh.kz и др.)

При создании сида использовать реалистичные имена, группы по специальностям (например, «Банк-1», «Учёт-2», «ПО-1»), одного директора и нескольких преподавателей и студентов.

---

## 9. Функциональные требования (из Заметки)

- **Главная:** новости и мероприятия списком (заголовок, описание, фото), поиск по дате/словам. CRUD — Директор/Админ.
- **Учебный процесс (студент):** журнал оценок, статус по кредитам/летнему семестру/задолженностям, расписание + ДЗ/уведомления, транскрипт/GPA, регламент/академический календарь/силлабус, преподаватели, библиотека (физический экземпляр, кто взял, бронь), техподдержка.
- **Социум:** форум (темы, ответы, анонимность только в форуме), чаты (общий, личные, групповые), карта здания, лента событий.
- **Профиль:** данные, статус, «Мои файлы» (папки, портфолио, лимит 2 ГБ), смена пароля/номера, язык, выход.
- **Директор:** профиль как публичный «Twitter» (шапка, фото, имя, описание, посты, закреплённые), кнопка «Написать директору».
- **Админ:** без страницы профиля директора, админ-панель, CRUD новостей/событий.

---

## 10. Использование контекста для ознакомления (onboarding)

**Для нового участника или нового чата — с чего начать:**

1. **Прочитать этот файл (`context.md`)** — разделы 1–3 (о проекте, роли, разделение Б/Ф), раздел 6 (структура репозитория), раздел 7 (БД кратко).
2. **Бэкенд-разработчик:** раздел 4 (чек-лист Б), `api/ENDPOINTS.md` (контракты API), `migrations/` (схема и сид). Запуск: `docker compose up -d`, `go run ./cmd/api`. Тестовые логины — см. ниже.
3. **Фронтенд-разработчик:** раздел 5 (чек-лист Ф), **`docs/FRONTEND_TUTORIAL.md`** (пошаговый туториал по задачам Фазы 2), `api/ENDPOINTS.md` (все эндпоинты и примеры JSON). Базовый URL API: `http://localhost:8080` (или переменная окружения на проде).
4. **Тестовые аккаунты (после сидинга):**  
   - director@nc.kz, admin@nc.kz — пароль `password123`  
   - teacher1@nc.kz, teacher2@nc.kz  
   - student1@nc.kz … student5@nc.kz  
   Пароль у всех: **password123**. Роли в ответе логина: `student`, `teacher`, `director`, `admin`.
5. **Авторизация в API:** после логина сохранять поле `token` из ответа и отправлять в заголовке: `Authorization: Bearer <token>` для всех запросов, кроме `POST /auth/login`. При 401 — перенаправить на экран входа.
6. **Репозиторий и ветки:** основной репозиторий — GitHub (college-system-app), ветка бэкенда — **Backend**, ветка фронтенда — **Frontend** (или по договорённости). Слияние в main — через Pull Request. Подробнее: `docs/GITHUB_TUTORIAL.md`.
7. **Дизайн:** макеты и прототипы из Figma — папка `design-prototypes/`; импорт и использование по мере готовности.

---

## 11. Инструкция по обновлению context.md

После каждого значимого изменения в проекте (новые эндпоинты, миграции, смена контрактов, этапы Б/Ф):

1. Открыть `context.md`.
2. Обновить раздел **«4. Фаза 1 — Участник Б»** (и при необходимости **«5. Фаза 2 — Участник Ф»**) — статусы шагов.
3. При изменении API — обновить `api/ENDPOINTS.md` и при необходимости раздел 7.
4. При добавлении миграций — кратко описать в разделе 7 или в списке миграций.
5. При смене структуры репозитория — обновить раздел 6.
6. Сохранить файл, чтобы в следующем чате контекст был актуальным.

---

*Последнее обновление контекста: добавлены раздел 10 «Использование контекста для ознакомления» (onboarding для Б/Ф), раздел 11 (инструкция по обновлению), туториал для фронтенда docs/FRONTEND_TUTORIAL.md (задачи 9–15 с шагами и привязкой к API). Фаза 1 (Б) выполнена. Тестовые логины: director@nc.kz, teacher1@nc.kz, student1@nc.kz и др., пароль password123.*
