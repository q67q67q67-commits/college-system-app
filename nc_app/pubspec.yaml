name: nc_app
description: Narxoz College — мобильное и веб-приложение для студентов и сотрудников.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  web_socket_channel: ^2.4.0
  file_picker: ^6.1.1
  path_provider: ^2.1.1
  open_filex: ^4.4.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
